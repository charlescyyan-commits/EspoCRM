# Regression Fixtures

These deterministic JSON fixtures define C14-era regression expectations for
email-lifecycle projection, ResearchEvidence persistence behavior, and
persistence-entity ACL roles. They contain no credentials, customer data, or
network targets. The regression tests consume them without a CRM, database, or
provider connection.
