# Workspace Scripts

Workspace-level operational scripts belong here. The extension release builder remains intentionally located at `crm-extension/scripts/build_release_package.ps1` so its manifest-relative path logic continues to work.
