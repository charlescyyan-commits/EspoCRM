# Api — reserved for Phase 3A-2.2

No API route definitions are implemented in Phase 3A-2.1.

Future intent (design only):

```text
POST /api/v1/prospecting-engine/import
```

Must remain a narrow, authenticated, one-way import endpoint.
