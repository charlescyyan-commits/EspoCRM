# Services — reserved for Phase 3A-2.2

No service PHP is implemented in Phase 3A-2.1.

Future intent:

- Contract schema + semantic gate validation
- Idempotency / duplicate resolution
- Lead Engine-owned field update
- ResearchEvidence snapshot import

Must not:

- Calculate scores
- Infer missing country or product data
- Overwrite CRM-owned ownership or activity fields
