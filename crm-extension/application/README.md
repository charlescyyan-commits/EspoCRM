# application/ placeholder

Reserved for future non-module application files.

This directory is intentionally outside EspoCRM's installable `files/` tree
so an empty placeholder is never copied over EspoCRM's real `application/` path.
