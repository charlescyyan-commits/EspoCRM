# EspoCRM Extension Tests

Run from repository root:

```bash
python -m unittest espocrm_extension.tests.test_extension_skeleton -v
```

These tests validate skeleton structure and Sync Contract field alignment only.
They do not connect to EspoCRM, a database, or the Prospecting Engine runtime.
