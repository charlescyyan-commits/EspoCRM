# scripts/ — reserved

EspoCRM extension install hooks may be added later:

- `BeforeInstall.php`
- `AfterInstall.php`
- `BeforeUninstall.php`
- `AfterUninstall.php`

Phase 3A-2.1 ships no install scripts and performs no CRM rebuild or DB mutation.
