# Deployment Assets

`railway/`, `docker/`, and `backup/` are intentionally empty operational boundaries. No production configuration or credentials were extracted. `provisioning/` contains the copied local EspoCRM role-provisioning script and must not be executed without an approved target.
